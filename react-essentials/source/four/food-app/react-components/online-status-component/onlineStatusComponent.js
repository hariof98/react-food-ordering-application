const OnlineStatusComponent = () => {
    const onlineStatusLayout = (
        <div>
            <h1>No Internet Connection!</h1>
            <h1>Try to reconnect before accessing.</h1>
        </div>
    );

    return onlineStatusLayout;
};

export default OnlineStatusComponent;
